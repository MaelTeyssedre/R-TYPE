/*
** EPITECH PROJECT, 2021
** R-TYPE
** File description:
** Packet
*/

#include "Packet.hpp"

Packet::Packet()
{
}

Packet::~Packet()
{
}

size_t Packet::pack(std::shared_ptr<std::vector<uint8_t>> data)
{
    size_t tmp = 0;

    return (tmp);
}

size_t Packet::pack(uint8_t *data, size_t size)  
{
    size_t tmp = 0;

    return (tmp);
}

std::shared_ptr<std::vector<uint8_t>> Packet::unpack()
{
    std::shared_ptr<std::vector<uint8_t>> tmp;

    return (tmp);
}