name: R-Type Doxygen

on:
  push:
    branches: 
      - main
      - develop
  pull_request:
    branches: 
      - main
      - develop

jobs:
  main:
    runs-on: ubuntu-latest

    steps:
    - name: Doxygen Action
      uses: Doxygen ./Doc/Doxyfile
